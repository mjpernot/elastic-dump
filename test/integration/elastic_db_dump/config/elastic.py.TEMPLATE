# Configuration file
# Classification (U)
# Unclassified until filled.

host = ["HOST_NAME1", "HOST_NAME2"]
port = 9200

# Name of the test repository name.
repo_name = "TEST_INTR_REPO"
# Name of the test repository directory path
# Must be a shared mount between all Elasticsearch database servers.
repo_dir = "REPO_DIRECTORY_PATH/TEST_INTR_REPO_DIR"
